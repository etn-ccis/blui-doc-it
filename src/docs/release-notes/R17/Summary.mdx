Updated our design pattern pages with modern images and restructured each page's content. Expanded the PX Blue stickersheet to include more PX Blue and Material UI components. Added reusable authentication workflow for React. Added new components for Angular (Nav Rail, Dropdown Toolbar) & React (Dropdown Toolbar), and React Native (Spacer, ListItemTag).
