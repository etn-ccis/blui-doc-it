import { DemoCard, ImageGrid, MaterialDesignDescription, Divider } from '../../app/components';
import PermanentImg from '../../app/assets/design-patterns/navigation/navigation-permanent.png';
// import TemporaryImg from '../../app/assets/design-patterns/navigation/navigation-temporary.png';
import PersistentImg from '../../app/assets/design-patterns/navigation/navigation-persistent.png';
import AnatomyImg from '../../app/assets/design-patterns/navigation/anatomy.png';
import Banner from '../../app/assets/design-patterns/navigation/banner.svg';
import CollapsibleImg from '../../app/assets/design-patterns/navigation/collapsible.png';
import ContentCoverDesktopImg from '../../app/assets/design-patterns/navigation/content-cover-desktop.png';
import ContentCoverTabletImg from '../../app/assets/design-patterns/navigation/content-cover-tablet.png';
// import DividerImg from '../../app/assets/design-patterns/navigation/divider.png';
import NavigationPermanentImg from '../../app/assets/design-patterns/navigation/navigation-permanent.png';
import NavigationPersistentImg from '../../app/assets/design-patterns/navigation/navigation-persistent.png';
import NavigationRailLabeledImg from '../../app/assets/design-patterns/navigation/navigation-rail-labeled.png';
import NavigationRailSpecsImg from '../../app/assets/design-patterns/navigation/navigation-rail-specs.png';
import NavigationRailImg from '../../app/assets/design-patterns/navigation/navigation-rail.png';
import NavigationTemporaryImg from '../../app/assets/design-patterns/navigation/navigation-temporary.png';
import NestedItemStandardsImg from '../../app/assets/design-patterns/navigation/nested-item-standards.png';
import NestedItemImg from '../../app/assets/design-patterns/navigation/nested-item.png';
import PanelSpecsImg from '../../app/assets/design-patterns/navigation/panel-specs.png';
// import ProductExample1 from '../../app/assets/design-patterns/navigation/Product Example 1.png';
// import ContentCover1 from '../../app/assets/design-patterns/navigation/Content Cover 1.png';
// import ContentCover2 from '../../app/assets/design-patterns/navigation/Content Cover 2.png';
// import GenericExample1 from '../../app/assets/design-patterns/navigation/Generic Example 1.png';
// import NestedItemExample from '../../app/assets/design-patterns/navigation/Nested Item Example.png';
// import NestedItemStandards from '../../app/assets/design-patterns/navigation/Nested Item Standards.png';
// import ExpandableAccountInfo from '../../app/assets/design-patterns/navigation/Expandable Account Information.png';
import DividerImage from '../../app/assets/design-patterns/navigation/Divider Image.png';

<!-- KEYWORDS: design pattern navigation drawer menu -->

# Navigation

<ImageGrid images={[Banner]} regularWidth />

Navigation refers to the way in which users access the different areas of your application and move from screen to screen. The most common forms of primary navigation are drawers and tabs.

<React.Fragment>
    <MaterialDesignDescription
        title={'Understanding Navigation'}
        url={'https://material.io/design/navigation/understanding-navigation.html#types-of-navigation'}
        description={`Read about Material Design's navigation fundamentals.`}
    />
    <MaterialDesignDescription
        title={'Navigation Drawer '}
        url={'https://material.io/components/navigation-drawer/'}
        style={{ marginRight: 0 }}
    />
    <Divider />
</React.Fragment>

# Drawer Navigation

A navigation drawer is a panel that is attached on the side of your application. These typically contain a [List](/patterns/lists) of primary pages in your application for the user to access quickly.

Most drawers have four main sections:

-   **Header**: contains...TODO
-   **Subheader**: contains...TODO
-   **Body**: contains...TODO
-   **Footer**: contains...TODO

There are several different styles of drawer navigation, each with slightly different behavior.

<DemoCard
    repository={'Drawer'}
    angular={'https://pxblue-components.github.io/angular/?path=/story/components-drawer--within-a-drawer-layout'}
    react={'https://pxblue-components.github.io/react/?path=/story/components-drawer--in-drawer-layout'}
/>

## Permanent

A permanent drawer is always visible and always open. The permanent drawer is typically full-width, as opposed to the rail variant (see below). Use this variation if you have:

-   five or more top-level pages
-   a nested navigation hierarchy
-   enough space to justify it (typically desktop-only applications)

<ImageGrid images={[NavigationPermanentImg]} regularWidth />

## Rail

A navigation rail is a permanently collapsed drawer. The rail is able to show less information than a standard drawer, but has the advantage of a more compact, space-saving package. Use this variation if you have:

-   fewer than seven top-level pages
-   a flat navigation hierarchy
-   limited space

<ImageGrid images={[NavigationRailImg]} regularWidth />

## Persistent

A persistent drawer is an always-visible drawer that can be opened and closed (expanded or collapsed). In its opened state, it looks and behaves like a permanent drawer. It the closed state, it looks and behaves like a navigation rail. The drawer is typically opened and closed by clicking an icon in the header. It can also be opened momentarily on desktop applications by hovering with the mouse. Use this variation if you have:

-   TODO

<React.Fragment>
    <ImageGrid
        images={[CollapsibleImg]}
        caption={[
            'Figure 1 displays the normal collapsed state of the menu.',
            'Figure 2 displays the open state of the menu.',
        ]}
        regularWidth
    />
    <ImageGrid
        images={[PersistentImg, PermanentImg]}
        caption={
            'When the user hovers on the Navigation Rail, the side navigation will expand temporarily, revealing the text details of each navigation item; when the user moves their cursor away, the whole side navigation panel collapses.'
        }
    />
</React.Fragment>

## Temporary

A temporary drawer is invisible until it is opened — typically by pressing a button in the [App Bar](/patterns/appbar). This variation of the drawer appears on top of the main navigation, typically using a dark overlay to block interaction with the main content. Use this variation if you have:

-   a mobile application
-   extreme space limitations

<React.Fragment>
    <ImageGrid images={[NavigationTemporaryImg]} regularWidth />
    <Divider />
</React.Fragment>

# Tab Navigation

> TODO: Image Missing

Tab navigation uses a horizontal bar with buttons for each navigation location. These can appear at the top or bottom of your application. Tabs can be used in conjunction with drawer navigation as a way to navigate between sub-pages (e.g., you may have several sub-pages on your main settings page).

<React.Fragment>
    <MaterialDesignDescription title={'Tabs'} url={'https://material.io/components/tabs'} />
    <MaterialDesignDescription title={'Bottom Navigation'} url={'https://material.io/components/bottom-navigation'} />
</React.Fragment>

# Behaviors

## Nested Navigation

When using drawer navigation, you can have multiple levels of navigation hierarchy. These appear as expandable panels in the drawer. When using a hierarchical navigation in the drawer, you should try to limit yourself to now more than two levels.

<ImageGrid images={[NestedItemImg, NestedItemStandardsImg]} regularWidth />

## Content-Cover vs. Content-Resize

When a drawer is opened, there are two ways to deal with the main application content.

With content-resize, the main application content resizes its area to accommodate the expanded drawer. This is the default behavior for most drawer variations.

With content-cover, the navigation panel appears on top of the main application content, partially obscuring it. This typically uses a dark overlay behind the drawer to direct the user's focus to the navigation panel and block interaction with the main content. This pattern is most common when using the temporary drawer variant on mobile and tablet applications.

<ImageGrid
    images={[ContentCoverDesktopImg]}
    caption={[
        'Figure 1 displays the normal collapsed state of the menu for desktop.',
        'Figure 2 displays the open state of the menu with a gray overlay.',
    ]}
    regularWidth
/>
<ImageGrid
    images={[ContentCoverTabletImg]}
    caption={[
        'Figure 1 displays the normal collapsed state of the menu for tablet.',
        'Figure 2 displays the open state of the menu with a gray overlay.',
    ]}
    regularWidth
/>

## Dividers

Divider lines can be used between navigation items in the drawer. They are most effective when used for:

-   separating nested drawer items
-   separating sections of the drawer contents

<ImageGrid
    images={[DividerImage]}
    regularWidth
    caption={['Avoid using divider lines between every navigation item. These can make the drawer appear busy.']}
/>

# Design Specifications

<ImageGrid images={[NavigationRailSpecsImg, NavigationRailLabeledImg, PanelSpecsImg]} regularWidth />

TODO: Tab specs if there are any differences from Material?

<Divider />

# Developers

Use the following components to implement this pattern:

**Angular** and **Ionic**:

-   Angular Material
    -   [Side Nav](https://material.angular.io/components/sidenav/overview)
    -   [Tabs](https://material.angular.io/components/tabs/overview)
-   @pxblue/angular-components
    -   [Drawer](https://pxblue-components.github.io/angular/?path=/info/components-drawer--readme)

**React**:

-   Material UI
    -   [Drawer](https://material-ui.com/components/drawers/)
    -   [Tabs](https://material-ui.com/components/tabs/)
-   @pxblue/react-components
    -   [Drawer](https://pxblue-components.github.io/react/?path=/info/components-drawer--get-read-me-story)

**React Native**:

-   @pxblue/react-native-components
    -   [Drawer](https://pxblue-components.github.io/react-native/?path=/info/components-documentation--drawer)
