import { ImageGrid, BLCIconButton } from '../../app/components';
import BarchartsComponent from '../../app/assets/visualizations/barcharts-component.png';
import { ReactComponent as BLCIcon } from '../../app/assets/visualizations/blcicon.svg';
import { ReactComponent as CodeIcon } from '../../app/assets/visualizations/codeicon.svg';

codeicon
# Bar Charts

Bar charts are used to categorize and compare datasets. Colors are used to represent 
different categories, while size is used to represent the category values.

### Component

<ImageGrid
    images={[BarchartsComponent]}
    regularWidth
/>


<BLCIconButton
    icon={BLCIcon}
    label={'View in Figma sticker sheet'}
    url={'/'}
/>
<BLCIconButton
    icon={CodeIcon}
    label={'View in Storybook'}
    url={'/'}
/>

